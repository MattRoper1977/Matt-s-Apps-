name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  publication:
    # Pinned 2026-09-06 reviewed Education completion publisher (Site PR #268); this workflow reads
    # this repository's current source commit and never edits source content.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@ff764b65508fcfee1c67b0bb38e32af86982269c
    with:
      builder_ref: ff764b65508fcfee1c67b0bb38e32af86982269c
