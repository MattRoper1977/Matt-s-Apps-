name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  publication:
    # Pinned 2026-09-06 shared-navigation publisher (Site PR #262); this workflow reads
    # this repository's current source commit and never edits source content.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@384c619956411393cc8a9b4e55e617114ddf175b
    with:
      builder_ref: 384c619956411393cc8a9b4e55e617114ddf175b
