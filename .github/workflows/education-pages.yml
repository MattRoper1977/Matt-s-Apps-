name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  publication:
    # Pinned 2026-09-07 reviewed Glitch handoff and all-file admission publisher (Site PR #292); this workflow reads
    # this repository's current source commit and never edits source content.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@09a44fae2b9f7f0e14b3fea8c0f0cd97fb80cd8a
    with:
      builder_ref: 09a44fae2b9f7f0e14b3fea8c0f0cd97fb80cd8a
