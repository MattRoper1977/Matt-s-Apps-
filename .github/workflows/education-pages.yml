name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  publication:
    # Pinned 2026-09-07 reviewed Science download registry publisher (Site PR #290); this workflow reads
    # this repository's current source commit and never edits source content.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@abb23325d065b3468ffcab756eb8bb4c8dd19c74
    with:
      builder_ref: abb23325d065b3468ffcab756eb8bb4c8dd19c74
