# HC1 HEALTH — 2026-09-06 (Apps repo, madebymatt.uk/Matt-s-Apps-/)

Token `mbm-two-domain-healthcheck-2026-09-06-HC1` · verdict **HC1_PARTIAL**. Estate
ledger and map: `Lessons/docs/HC1_HEALTH_2026-09-06.md`, `Lessons/docs/ESTATE_MAP_2026-09-06.md`.
Measured at main `96d160f0`; published with Site builder `ff764b65`, Lessons pin `5ba6271`.

## Served proof at HEAD
`Education Pages publication` run 34038531016 deployed (job 101501121788).
`Made by Matt cross-estate unification` live-proof green at `96d160f` (run 34038530670);
the reds at `9a96172`, `9b2d933`, `b83cdec` were the verifier reading the raw menu
instead of the published one, fixed by #34; one upload failed on runner DNS
(`EAI_AGAIN`), not this repo.

## Measured on the rebuilt apps tree (106 files, 60 html)
- 0 loads from `madebymatt-play.uk`; 1 labelled play link (`index.html`). Third-party
  subresource: `PDF_Studio.html` loads two scripts from `cdnjs.cloudflare.com`
  (pre-existing; adult tool). Ko-fi links on 16 adult tools (builder `APP_ADULT`).
- Runtime: 60/60 load, 0 console errors, 0 failed requests.
- **Zoom blocked on 19 studios** (`user-scalable=no` / `maximum-scale=1`):
  `Exit_Ticket`, `Video_Studio`, `Seating_Studio`, `Feelings_Checkin`, `Rubric_Studio`,
  `Quiz_Studio`, `Writing_Frames`, `Classroom_Toolkit`, `Animation_Studio`,
  `Mindmap_Studio`, `Art_Studio`, `Design_Studio` and 7 more. Order §2.2: remove the
  block and add `touch-action: manipulation` where a canvas needs it. HELD as a
  proposal: the drawing studios need real touch testing first (R5).
- The four science investigations (`orbit-vector-diagnostic`, `enzyme-reactor-overdrive`,
  `wave-interference…`, `ohms-law-fault-finder…`) trip the game-loop heuristic and are
  ruled educational in Site `education_policy.py`; classified APP.
- Storage keys are per-studio (`matt_whiteboard_v2`, `quizdecks`, `seating`, …); nothing
  expects a play-origin key.

Rollback: main `96d160f0326160ab88da301ec28d0130b6c446fb`.
