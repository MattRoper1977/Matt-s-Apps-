name: Education Pages publication
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  publication:
    # Pinned 2026-09-06 education-only publisher (Site PR #263); this workflow reads
    # this repository's current source commit and never edits source content.
    uses: MattRoper1977/mattroper1977.github.io/.github/workflows/education-publication.yml@0d5858e672e95505d344a0e408b8536997b590a0
    with:
      builder_ref: 0d5858e672e95505d344a0e408b8536997b590a0
