# PIN1 Apps per-gate transaction — 12 September 2026

mbm-pin-coverage-PIN1-TOP

## Decision and boundary

Implement original PIN1 §2.1 for each Apps gate, not paired coverage as a
substitute. Gate C previously covered 5/9 assertions; Gate D 4/9; their union
already covered 9/9. This is not nine previously unguarded files.

Derive exact paths from CANONICAL_HASHES, the applicable MANIFEST_PINS,
LUNDYLOOP_CI_PINS and PUBLICATION_CALLER_PATH. Preserve all existing non-digest
subjects, including Gate D's three payload/tool globs. The only new non-pin
dependencies are the two exact derivation/control scripts. Neither gate gains
an estate-wide glob. CI checks and refuses stale output; it never regenerates
or re-pins to make a check pass.

| Gate | Asserted pins | Exact pin coverage after | Total paths per PR/push |
|---|---:|---:|---:|
| C: cross-estate | 9 | 9 | 16 |
| D: LundyLoop | 9 | 9 | 17 |

Nine assertions: assets/mbm-platform.css, assets/mbm-platform.js,
assets/mbm-theme.js, assets/mbm-hub.css, apps.json,
.github/workflows/education-pages.yml,
.github/workflows/verify-lundyloop-professional-os.yml,
tools/lundyloop/verify_live_bytes.py, tools/lundyloop/test_live_bytes.py.

Use `python3 tools/pin1/derive_triggers.py --write` in the Apps checkout after a
registry change, then review the generated workflow and update its digest in
both shared checker copies. `--check` verifies both gates at both PR and push
events, including additions/deletions and exact non-pin dependency sets.
Digest-only changes do not change generated paths, so the workflow pin has a
stable result after regeneration.

## Coordinated owner-first landing

Starting Lessons main: d019b30b6df251bcd18923e391d6955c8d564ddd.
Starting Apps main: c9a7a4e3496b0f1449ad035b4f77c3d6d7dbc56a.

Lessons owns tools/verify_cross_estate_unification.py. Prepare byte-identical
checker candidates in both repositories. Land its isolated checker update in
Lessons first, complete its publication, then land the Apps implementation and
complete its publication. The temporary between-merge state is explicit:
Lessons carries the forthcoming Apps workflow digest, unused in Lessons mode;
Apps retains its old checker and matching old workflow until its atomic commit.
No checker may be selectively swapped to satisfy a local command. Verify final
main-to-main identity after both PRs.

The checker delta is one LundyLoop workflow digest plus two exact internal
checker-tool boundary entries. All digest/wording/canonical/payload predicates
remain unchanged. New workflow digest:
6d0237c9756954b07cfd4d2752136b78fb766d3c72d4f66342f7d6374e0f4fe2.
Previous digest:
79f2df4bab2df52719160ccf9d1fe8d0ae543555778ab7d3a660094dfb0796d0.

Lessons #456 remains open at c4e2390515c8e15182b21a323913fb955f671c91.
It touches this shared file but only CANONICAL_HASHES and catalogue token/page
pins, disjoint from this transaction's LundyLoop and internal-tool entries.
Do not merge, rebase, repin, or reuse #456. It needs its own later preflight.

## Local evidence before opening PRs

- 15/15 trigger derivation controls pass. Every mutation asserts it landed.
- Both Apps gates: 9/9 exact pin triggers in PR and push, freshness PASS.
- Lessons' existing derivation remains 440 assertions / 446 triggers, PASS.
- Apps and Lessons existing full static contracts and self-tests pass.
- LundyLoop static payload: 8 HTML, 6 inline scripts, 34 links, 22 manifest
  records; PASS, 0 failures/warnings. Its planted defect fires.
- All 7 existing LundyLoop live-byte/provenance controls pass.
- Independent YAML comparison: only path blocks and added freshness/control
  commands differ; permissions, other events, conditions, existing jobs and
  assertion commands remain unchanged.
- Independent checker AST comparison: only the reviewed workflow pin and two
  named internal-tool boundary entries differ.
- Both checker candidates are byte-identical.
- Publication filtering excludes all changed workflow/tool/review-record paths;
  no runtime, payload, catalogue, logo, design, caller pin or admission entry
  changes in this transaction.

These are local proofs, not GitHub event experiments or deployed evidence.

## Still required before closing the Apps pass

Review exact-head CI and inherited failures by evidence; land in the sequence
above; confirm each publication's deploy job succeeds. Then run Apps scratch
RED (pinned-file-only stale digest), GREEN (same edit with corrected digest),
and DORMANT (unrelated file only). Keep the unrelated control outside existing
payload globs; close all scratch PRs unmerged. Record actual run/job references,
not estimates, on the production PRs.

PIN1 overall remains PARTIAL until an actual scheduled UX2 event containing
#515 retains the nine-proof census. Push/manual results cannot substitute.
No overnight wait or new automation is introduced here. SB1 #516 and Lessons
trigger repair #517 are complete and not repeated. LP1, #456 and SW2 design
remain held/outside this pass.

Rollback references are the two starting main SHAs above. Reverting Apps must
restore its workflow and checker digest together; the matching owner mirror
must be reviewed too. Never reset main or silently move publication pins.

PIN1_PARTIAL

mbm-pin-coverage-PIN1-BOTTOM
